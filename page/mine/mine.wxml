<view class="container">
	<view class="header">
		<image class="avatar" src="{{userInfo.avatarUrl}}" />
		<view class="nickname">{{userInfo.nickName}}</view>
	</view>
	<view class="section" wx:for="{{mode}}" wx:key="id">
    <navigator  url="{{item.pagePath}}">
		  <view class="line judgement">{{item.text}}</view>
    </navigator>
	</view>
	<view class="section">
		<view class="line judgement">我的钱包<text class="tl">余额：￥0</text></view>
	</view>
</view>
